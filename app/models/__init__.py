"""
Models Package

Contains SQLAlchemy ORM models that define the database schema.
These models represent the data structure and relationships in your database.

Future models might include:
- user.py: User account and profile models
- message.py: Chat message models
- room.py: Chat room/channel models
- base.py: Base model class with common fields (id, created_at, updated_at)

Example usage:
    from app.models.user import User
    from app.models.message import Message
"""